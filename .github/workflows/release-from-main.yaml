name: Create Release (Deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Release Please now creates GitHub releases directly."
